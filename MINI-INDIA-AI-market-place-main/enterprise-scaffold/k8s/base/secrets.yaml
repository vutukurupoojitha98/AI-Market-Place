# External Secrets Operator ready. In prod use SealedSecrets / SOPS / AWS SM.
apiVersion: v1
kind: Secret
metadata: {name: minimart-secrets}
type: Opaque
stringData:
  db-url: jdbc:postgresql://postgres:5432/minimart
  db-user: minimart
  db-pass: change-me
  jwt-secret: change-me
  stripe-key: sk_test_replace
  openai-key: sk-replace
